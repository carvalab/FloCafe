/**
 * Global default package entry. Catalog and runtime imports stay separate.
 */

import { GLOBAL_DEFAULT_MANIFEST } from './manifest';
import { GLOBAL_DEFAULT_RUNTIME_BUNDLE } from './runtime';

export { GLOBAL_DEFAULT_MANIFEST, GLOBAL_DEFAULT_RUNTIME_BUNDLE };
