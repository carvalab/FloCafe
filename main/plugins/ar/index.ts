/**
 * Argentina package entry. Catalog and runtime imports stay separate.
 */

import { AR_MANIFEST } from './manifest';
import { AR_RUNTIME_BUNDLE } from './runtime';

export { AR_MANIFEST, AR_RUNTIME_BUNDLE };
export * from './mercado-pago';
