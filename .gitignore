node_modules/
dist/
release/
*.db
*.db-shm
*.db-wal
# Intentional exception: a real committed DB fixture for the upgrade-path
# regression test (tests/upgrade-path.test.ts, specs/DatabaseMigrations.md).
!tests/fixtures/upgrade-snapshots/*.db
backups/
.env
.env.local
.DS_Store
.claude/
.agents/
docs/superpowers/
.superpowers
.gstack

# Local pentest reports/PoCs — never commit
vuln/

# Apple signing assets — NEVER commit
xcode/
build/flo.provisionprofile
*.cer
*.p12
*.p8
*.certSigningRequest
*.provisionprofile
*.mobileprovision

.next/
brain/
